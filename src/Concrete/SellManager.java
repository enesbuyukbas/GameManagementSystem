package Concrete;

import Abstract.ASellManager;
import Entity.Gamer;

public class SellManager extends ASellManager {
    @Override
    public void sell(Gamer gamer) {
        super.sell(gamer);
    }
}
